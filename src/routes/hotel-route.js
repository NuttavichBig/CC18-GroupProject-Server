const express = require("express")
const router = express.Router()


router.get('/',()=>{}) // query
router.get('/:hotelId',()=>{})
router.post('/',()=>{}) // authen
router.patch('/:hotelId',()=>{}) // authen
router.delete('/:hotelId',()=>{}) // authen



module.exports = router
